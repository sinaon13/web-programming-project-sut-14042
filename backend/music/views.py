# TODO [Commit 2]: Implement views for:
#   - TrackListCreateView (GET list + POST upload, artist only for POST)
#   - TrackDetailView (GET/PATCH/DELETE, artist owns for write)
#   - TrackStreamView (POST /tracks/<id>/stream/ — logs stream, enforces daily limit)
#   - AlbumListCreateView (GET list + POST create, artist only for POST)
#   - AlbumDetailView (GET/PATCH/DELETE)
